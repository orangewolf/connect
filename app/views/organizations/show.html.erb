<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @organization.title %>
</p>

<p>
  <strong>Tagline:</strong>
  <%= @organization.tagline %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @organization.image.url(:medium), width: '100' %></p>

<%= link_to 'Edit', edit_organization_path(@organization) %> |
<%= link_to 'Back', organizations_path %>
